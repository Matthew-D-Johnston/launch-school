Locate the ruby documentation for methods `File::path` and `File#path`. How are they different?

**Differences:**

1. `File::path` is a class method, while `File#path` is an instance method.
2. `File::path` returns a _string_, while `File#path` returns a _filename_.